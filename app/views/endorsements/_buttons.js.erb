document.getElementById("idea-<%= @idea.id %>-endorsements").innerHTML = "<%=
	# This might be called from controllers other than EndorsementsController, so
	# use full path for partial.
	escape_javascript render(
		:partial => "endorsements/buttons",
		:formats => :html,
		:locals => {:idea => @idea}
	)
%>"
